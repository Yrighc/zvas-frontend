/**
 * 苹果风（极简精密控制台）样式约束令牌
 */
import { TABLE_CLASS_NAMES } from "@/components/table/tableClassNames"

/**
 * 仪表盘标准表格类配置 (Apple Dashboard Standard)
 * 适用于系统中绝大多数数据表格展示场景，统一表头、间距及悬停高亮逻辑。
 * 注意：使用时应手动传入 `base` 类的 `min-w-[XXXpx]` 等容器宽度限制。
 */
export const APPLE_TABLE_CLASSES = TABLE_CLASS_NAMES
